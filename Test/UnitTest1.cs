using App;

namespace Test
{
    [TestClass]
    public class RomanNumberTest
    {
        //IT WAS 1st DZ
        /*[TestMethod]
        public void ParseTest()
        {
            /*var testCases = new Dictionary<string, int>()
            {
                { "N", 0 },
                { "I", 1 },
                { "II", 2 },
                { "III", 3 },
                { "IIII", 4 },
                { "IV", 4 },
                { "VI", 6 },
                { "D", 500 },
                { "M", 1000 },
            };
            foreach (var testCase in testCases)
            {
                RomanNumber rn = RomanNumber.Parse(testCase.Key);
                Assert.IsNotNull((rn));
                Assert.AreEqual(testCase.Value, rn.Value, $"{testCase.Key} -> {testCase.Value}");
            }
        }

        [TestMethod]
        public void DigitValueTest()
        {
            var useCases = new Dictionary<string, int>()
            {
                { "N", 0 },
                { "I", 1 },
                { "V", 5 },
                { "X", 10 },
                { "L", 50 },
                { "C", 100 },
                { "D", 500 },
                { "M", 1000 },
            };
            foreach (var testCase in useCases)
            {
                Assert.AreEqual(
                    testCase.Value,
                    RomanNumber.DigitValue(testCase.Key),
                    $"{testCase.Key} -> {testCase.Value}");
            }
        }#1#
    */
    }
}
